import {
  describe,
  expect,
  test
} from '@jest/globals'

import { KJS } from '../'

describe('KJS unit tests', () => {
  test('adios', () => {
    // KJS.Scene.load()
    expect(1).toBe(1)
  })
})
